@model TravelDesk.Models.ApplicationRequestHistory

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>ApplicationRequestHistory</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ApplicationRequestHistoryId" />
            <div class="form-group">
                <label asp-for="RequestStatus" class="control-label"></label>
                <input asp-for="RequestStatus" class="form-control" />
                <span asp-validation-for="RequestStatus" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ApplicationRequestId" class="control-label"></label>
                <select asp-for="ApplicationRequestId" class="form-control" asp-items="ViewBag.ApplicationRequestId"></select>
                <span asp-validation-for="ApplicationRequestId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
